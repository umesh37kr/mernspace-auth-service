function login(user_name: string): string {
    // console.log("username")
    const user = {
        name: 'umesh',
    };
    const userdata = user.name;
    return user_name + userdata;
}

login('umeshkr');
